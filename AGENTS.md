# 出图设计规范

本仓库所有 Puppeteer 渲染图遵循“Steam 信息面板 + 群聊快速浏览”的方向。以 `900px` 为设计基准宽度，并始终兼容 `imageWidth` 配置；不要将固定像素尺寸当作唯一布局依据。

## 视觉基线

- 使用深蓝渐变作为页面背景。所有页面共享同一色系，只允许轻微调整渐变方向和暗部比例。
- 页面保持紧凑：优先使用网格、细分隔线、低对比色块与合理行距组织信息，不堆叠大留白或浮动卡片。
- 所有矩形均使用直角 `border-radius: 0`。只有语义本身为圆形的头像、序号徽章等可使用 `border-radius: 50%`。
- 页头可使用极弱投影；列表、分区、封面和普通信息块不使用投影。游戏封面与排行缩略图使用直角冷青细边框。
- 冷青承担结构与链接强调。成功使用绿、警告使用黄、错误使用红、折扣使用橙/绿；不要为普通内容引入额外主色。
- 状态卡可以保留用户个人资料背景和圆形头像，但必须叠加稳定的深蓝黑遮罩，确保任何背景下文字均清晰可读。

## 文字与页脚

- 默认沿用可配置的霞鹜文楷。标题靠字重、字号、英文品牌字和分隔线建立层级，不混入第二套标题字体。
- emoji 可用于标题、分区、标签与状态，但单个信息标签最多一个，不能替代清晰的文字语义。
- 页头品牌固定为 `KOISHI-PLUGIN-STEAM-VINCENTZYU`，不使用 Steam 官方图标作为插件标识。
- 每张图片页脚必须包含插件名、真实数据来源和 `UTC+8` 完整生成时间，并保留简短的非官方声明。

## 新页面验收

- 用默认宽度和最长中文游戏名验证，文字、价格、emoji 与封面不得溢出或遮挡。
- 长列表必须依赖配置项控制显示数量，图片中清楚标识已展示条目和总条目；不得用 CSS 静默裁切数据。
- 截图前检查 `.sheet` 覆盖整个图片高度，背景不得在底部或空白区域断裂为 `body` 的纯色。
- 修改渲染逻辑或样式后，至少验证帮助、绑定列表、库存/排行、商店特惠与状态卡，并运行 TypeScript 检查和插件构建。
